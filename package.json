{
  "name": "dex-connector-ui-sdk",
  "version": "0.1.0",
  "description": "Harness-agnostic agent capability SDK: turn raw tool-call results (builtin tools + MCP connectors) into structured UI descriptors. Pure data — the host renders.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "tsc --noEmit",
    "test": "vitest run"
  },
  "keywords": [
    "agent",
    "llm",
    "mcp",
    "tool-results",
    "generative-ui",
    "connector",
    "extractors"
  ],
  "license": "UNLICENSED",
  "private": true,
  "devDependencies": {
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
